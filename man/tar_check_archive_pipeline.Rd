% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tar_check_archive.R
\name{tar_check_archive_pipeline}
\alias{tar_check_archive_pipeline}
\alias{tar_check_archive_name}
\title{Check a pipeline or target name}
\usage{
tar_check_archive_pipeline(
  pipeline,
  package,
  envir = parent.frame(),
  arg_nm = rlang::caller_arg(pipeline),
  error_call = rlang::caller_env()
)

tar_check_archive_name(
  name,
  package,
  pipeline,
  envir = parent.frame(),
  arg_nm = rlang::caller_arg(name),
  error_call = rlang::caller_env()
)
}
\arguments{
\item{pipeline, name}{A scalar character of the pipeline or target name to
check.}

\item{package}{A scalar character of the package name.}

\item{envir}{An environment used to resolve \code{package}, so that a package
currently loaded with \code{pkgload::load_all()} (e.g. during interactive
package development) is resolved to its source directory instead of its
installed one. Defaults to the calling environment.}

\item{arg_nm, error_call}{Passed to \code{\link[rlang:arg_match0]{rlang::arg_match0()}} to attribute the
error to the correct argument and call.}
}
\value{
\code{pipeline} or \code{name}, invisibly, if valid. Otherwise, throws an
error.
}
\description{
Validates \code{pipeline} against the pipelines bundled in \code{package} (see
\code{\link[=tar_archive_pipelines]{tar_archive_pipelines()}}), or \code{name} against the targets defined in a
pipeline (see \code{\link[=tar_manifest_archive]{tar_manifest_archive()}}). Errors with a "did you mean"
suggestion when the value doesn't match, so callers don't have to
reimplement this check themselves.
}
\examples{
tar_check_archive_pipeline("example-model", package = "tarchives")

\donttest{
withr::with_envvar(
  c(R_USER_CACHE_DIR = tempfile()),
  tar_check_archive_name(
    "data",
    package = "tarchives",
    pipeline = "example-model"
  )
)
}

}
\seealso{
\code{\link[=tar_archive_pipelines]{tar_archive_pipelines()}} to list the pipelines in a package, and
\code{\link[=tar_manifest_archive]{tar_manifest_archive()}} to list the targets within a pipeline.
}
